


void main() {
//   testWidgets('Changing pages in the Navigation bar', (WidgetTester tester) async {
//         // Build the app
//         await tester.pumpWidget(
//           const MediaQuery(
//             data: MediaQueryData(),
//             child: MaterialApp(home: currentPage()),
//           ),
//         );
//
//
//         // Verify that the initial page is the Home page
//
//         expect(find.byKey(const Key('home_page')), findsOneWidget);
//         expect(find.byKey(const Key('alarms_page')), findsNothing);
//         expect(find.byKey(const Key('profile_page')), findsNothing);
// /*
//         // Tap the Alarms tab
//         await tester.tap(find.widgetWithText(BottomNavigationBarItem, "Alarms"));
//         await tester.pumpAndSettle();
//
//         // Verify that the current page is the Alarms page
//         expect(find.byKey(const Key('home_page')), findsNothing);
//         expect(find.byKey(const Key('alarms_page')), findsOneWidget);
//         expect(find.byKey(const Key('profile_page')), findsNothing);
//
//         // Tap the Profile tab
//         await tester.tap(find.byWidgetPredicate((widget) => widget is BottomNavigationBarItem && (widget as BottomNavigationBarItem).label == 'Profile'));
//         await tester.pumpAndSettle();
//
//         // Verify that the current page is the Profile page
//         expect(find.byKey(const Key('home_page')), findsNothing);
//         expect(find.byKey(const Key('alarms_page')), findsNothing);
//         expect(find.byKey(const Key('profile_page')), findsOneWidget);
//         */
//       });
}